# Direct-dependency baseline; CI checks Python 3.10 and 3.12.
# Package consumers use compatible ranges declared in setup.py.
# Transitive versions are captured with the release build's pip-freeze.txt.
numpy==2.1.3
PySide6==6.11.1
matplotlib==3.10.8
compas==2.15.1
compas_viewer==2.0.2
pytest==9.1.1

# Optional for STEP preview support (recommended via conda-forge):
# conda create -n tolforge -c conda-forge python=3.10 compas_occ compas_viewer
# conda activate tolforge
# python -m pip install -e .
# Native CAD packages are deliberately separate from the pip-only executable.
